<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ruptech.firefighting">

    <!-- To auto-complete the email text field in the login form with the user's emails -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".App"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.MainActivity"
            android:label="@string/title_activity_actionbar"
            android:launchMode="singleTop" />
        <activity
            android:name=".detail.MaintainActivity"
            android:label="@string/title_item_maintain"
            android:launchMode="singleTop"
            android:parentActivityName=".main.MainActivity" />
        <activity
            android:name=".detail.CheckActivity"
            android:label="@string/title_item_check"
            android:launchMode="singleTop"
            android:parentActivityName=".main.MainActivity" />
        <activity
            android:name=".detail.PlanDetailActivity"
            android:label="@string/title_item_check"
            android:launchMode="singleTop"
            android:parentActivityName=".detail.CheckActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/action_settings"
            android:launchMode="singleTop" />
        <activity
            android:name=".detail.WorklogActivity"
            android:label="@string/tab_title_detail_worklog"
            android:launchMode="singleTop"
            android:parentActivityName=".detail.MaintainActivity" />
        <activity
            android:name=".detail.WorkHourActivity"
            android:label="@string/tab_title_workhour"
            android:launchMode="singleTop"
            android:parentActivityName=".detail.WorklogActivity" />
        <activity
            android:name=".detail.ItemActivity"
            android:label="@string/tab_title_detail_item"
            android:launchMode="singleTop"
            android:parentActivityName=".detail.MaintainActivity" />
    </application>

</manifest>
